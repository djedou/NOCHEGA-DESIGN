/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/NOCHEGA-DESIGN",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
};

export default nextConfig;
