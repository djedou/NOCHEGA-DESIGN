/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/NOCHEGA-DESIGN",
    output: "export",
    reactStrictMode: true,
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
